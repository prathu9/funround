import { Inter, Archivo, Poppins } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });
export const archivo = Archivo({subsets: ['latin']});
export const poppins = Poppins({subsets: ['latin'], weight: ["500", "900"]});